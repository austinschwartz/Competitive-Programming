#include <cstdio>
#include <cstdlib>
#include <string.h>

using namespace std;

int main() {
  int s;
  char* n;
  for (;;) {
    scanf("%d %s\n", &s, n);
    if (s == 0 && n == 0) break;
    for (int i = 0; i < 2*s + 3; i++) {


    }
  }
}
